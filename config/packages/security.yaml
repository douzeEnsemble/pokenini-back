security:
  password_hashers:
    Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: "auto"

  providers:
    users:
      id: App\Security\UserProvider

  firewalls:
    dev:
      pattern: ^/(_(profiler|wdt)|css|images|js)/
      security: false
    main:
      lazy: true
      stateless: true
      provider: users
      access_token:
        token_handler: App\Security\AccessTokenHandler
        token_extractors: header

  access_control:
    - { path: ^/istration, roles: ROLE_ADMIN }
    - { path: ^/user, roles: PUBLIC_ACCESS }
    - { path: ^/album, methods: [GET], roles: PUBLIC_ACCESS }
    - { path: ^/album, methods: [PATCH, POST, PUT], roles: ROLE_USER }
    - { path: ^/labels, roles: PUBLIC_ACCESS }
    - { path: ^/credits, roles: PUBLIC_ACCESS }
    - { path: ^/, roles: ROLE_USER }
